import { MayusValidatorDirective } from './mayus-validator.directive';

describe('MayusValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MayusValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
